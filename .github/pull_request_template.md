## Summary

<!-- One or two sentences: what does this PR do and why? -->

## Changes

<!-- Bullet list of the concrete changes in this PR -->
-

## Testing Performed

<!-- How did you verify this works? e.g. `pytest -v`, manual curl against /health and /predict, local `docker build` + `docker run` -->
-

## Docker Impact

<!-- Does this change the Dockerfile, requirements, exposed port, base image,
     or anything else that affects the built image? If none, say "No Docker
     impact." -->

## Checklist

- [ ] Application runs locally
- [ ] Tests pass locally (`pytest`)
- [ ] Docker image builds successfully (`docker build .`)
- [ ] No credentials are committed
- [ ] API health endpoint works (`GET /health`)
- [ ] Code is ready for review
